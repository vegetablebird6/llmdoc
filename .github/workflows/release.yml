name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: Publish npm package and GitHub release
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.2.2
        with:
          fetch-depth: 0

      - name: Use Node.js 24
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          # Trusted publishing(OIDC)需要 npm >= 11.5.1;Node 24 内置 npm 11
          node-version: 24
          registry-url: https://registry.npmjs.org
          cache: npm

      - name: Install and verify
        run: |
          npm ci
          npm run typecheck
          npm run lint
          npm test
          npm run test:integration
          npm run build
          npm run validate:dogfood
          npm run check:prompts

      - name: Verify release versions
        shell: bash
        run: |
          set -euo pipefail
          expected="${GITHUB_REF_NAME#v}"
          cli_version="$(node -p "require('./cli/package.json').version")"
          root_version="$(node -p "require('./package.json').version")"
          claude_version="$(node -p "require('./.claude-plugin/plugin.json').version")"
          codex_version="$(node -p "require('./.codex-plugin/plugin.json').version")"
          # acplugin 以 marketplace.json 的 plugins[].version 为插件版本来源,漏 bump 曾导致生成物版本漂移
          marketplace_version="$(node -p "require('./.claude-plugin/marketplace.json').plugins[0].version")"
          test "${cli_version}" = "${expected}"
          test "${root_version}" = "${expected}"
          test "${claude_version}" = "${expected}"
          test "${codex_version}" = "${expected}"
          test "${marketplace_version}" = "${expected}"

      - name: Pack npm and plugin artifacts
        id: artifacts
        shell: bash
        run: |
          set -euo pipefail
          npm_tarball="$(cd cli && npm pack --silent)"
          mv "cli/${npm_tarball}" "${npm_tarball}"
          plugin_tarball="llmdoc-plugin-${GITHUB_REF_NAME}.tar.gz"
          git archive --format=tar.gz --output="${plugin_tarball}" "${GITHUB_REF_NAME}"
          echo "npm_tarball=${npm_tarball}" >> "${GITHUB_OUTPUT}"
          echo "plugin_tarball=${plugin_tarball}" >> "${GITHUB_OUTPUT}"

      - name: Publish @vegetablebird6/llmdoc to npm (trusted publishing)
        shell: bash
        # 无 token:npmjs.com 上将本仓库的 release.yml 配置为 trusted publisher,
        # npm 通过 OIDC(顶部 id-token: write)换取短期凭证;provenance 自动附带。
        run: |
          set -euo pipefail
          expected="${GITHUB_REF_NAME#v}"
          # fork 版本使用 prerelease(如 3.6.0-ng)，npm 要求显式 --tag；稳定版仍走 latest
          dist_tag=latest
          if [[ "${expected}" == *-* ]]; then
            dist_tag="${expected#*-}"
            dist_tag="${dist_tag%%.*}"
          fi
          # 幂等:tag 重跑时该版本可能已在 registry,跳过而不是 409 失败
          if npm view "@vegetablebird6/llmdoc@${expected}" version >/dev/null 2>&1; then
            echo "@vegetablebird6/llmdoc@${expected} already published — skipping"
          else
            npm publish "${{ steps.artifacts.outputs.npm_tarball }}" --access public --tag "${dist_tag}"
          fi

      - name: Build release notes
        id: notes
        shell: bash
        run: |
          set -euo pipefail

          tag="${GITHUB_REF_NAME}"
          previous_tag="$(git tag --sort=-creatordate | grep -v "^${tag}$" | head -n 1 || true)"

          {
            echo "## Changes"
            echo
            if [ -n "$previous_tag" ]; then
              git log --pretty='- %s (%h)' "${previous_tag}..${tag}"
            else
              git log --pretty='- %s (%h)' "${tag}"
            fi
            echo
            echo "## Changed Files"
            echo
            if [ -n "$previous_tag" ]; then
              git diff --name-only "${previous_tag}..${tag}" | sed 's/^/- /'
            else
              git show --name-only --pretty='' "${tag}" | sed '/^$/d; s/^/- /'
            fi
          } > release-notes.md

      - name: Publish release
        uses: softprops/action-gh-release@6cbd405e2c4e67a21c47fa9e383d020e4e28b836 # v2.3.3
        with:
          body_path: release-notes.md
          files: |
            ${{ steps.artifacts.outputs.npm_tarball }}
            ${{ steps.artifacts.outputs.plugin_tarball }}
